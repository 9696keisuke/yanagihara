import React from "react";
import MainTemplate from "../templete/Maintemplete";

const Good: React.FC = (props) => {
  return <MainTemplate title="good" content={<h1>よい</h1>} />;
};

export default Good;
